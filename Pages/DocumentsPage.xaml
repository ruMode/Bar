<Page x:Class="WpfApp1.Pages.DocumentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="860" d:DesignWidth="1350"
      Title="EmployeesPage" Background="Transparent">

    <Grid Background="Transparent">
        <TextBlock Text="Тут должны быть документы" FontSize="48" VerticalAlignment="Center" HorizontalAlignment="Center"/>

        <ListView>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding}"/>
                        <TextBlock Text="{Binding }" FontSize="14" Margin="20 0"/>
                        <TextBlock Text="{Binding }" FontSize="14" Margin="20 0"/>
                        <TextBlock Text="{Binding }" FontSize="14" Margin="20 0"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
